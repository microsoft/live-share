#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../markdownlint-cli/markdownlint.js" "$@"
  ret=$?
else 
  node  "$basedir/../markdownlint-cli/markdownlint.js" "$@"
  ret=$?
fi
exit $ret
